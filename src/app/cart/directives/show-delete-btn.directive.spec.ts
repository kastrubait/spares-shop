import { ShowDeleteBtnDirective } from './show-delete-btn.directive';

describe('ShowDeleteBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDeleteBtnDirective();
    expect(directive).toBeTruthy();
  });
});

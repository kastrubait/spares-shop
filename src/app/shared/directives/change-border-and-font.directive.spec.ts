import { ChangeBorderAndFontDirective } from './change-border-and-font.directive';

describe('ChangeBorderAndFontDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBorderAndFontDirective();
    expect(directive).toBeTruthy();
  });
});
